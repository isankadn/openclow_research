# 2026-03-30

- User changed the standing default ClickHouse xAPI database from `saikyo_old` to `saikyo_new`.
- This should be treated as the permanent default for future xAPI exploration and analysis until the user explicitly changes it again.
- Historical notes, analyses, and project files that genuinely refer to `saikyo_old` should remain as historical records unless the user asks to migrate/update them.
- User added a new canonical CSV of operation names/definitions at `/home/ubuntu/.openclaw/external-resources/new-operation-names.csv`.
- Updated saved memory so this new CSV is the only operation-name source to use going forward.
- User explicitly said there is no need to keep the old names; use the new names and definitions only.
- Preserve CSV wording as-given for now, including entries with missing definitions and minor spelling issues, unless the user asks for a cleaned normalized glossary.
- Added new remembered `statements_mv` column definitions: `platform` = platform name (for example BookRoll, Moodle, Logpalette, GOAL, SCROLL), `actor_name_role` = user role, `context_id` = Moodle course ID, `context_title` = Moodle course name, and `context_label` = Moodle course name.
- Added a saved LEAF student-flow interpretation: Moodle (LMS) login/authentication -> Moodle course -> BookRoll via LTI -> read course-linked contents -> optionally view Analysis.
- Saved the rule that viewed content is assigned to a Moodle course and that Moodle courses are categorized by school grade/level.
- Saved the provided Moodle SQL query pattern for mapping parent categories, child categories, and course names.
