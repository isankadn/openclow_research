# 2026-03-25

- Added a LEAF-wide research organization preference: each research effort should keep its data, documents, notes, code, and outputs in a separate folder.
- Project-specific artifact locations should be remembered/recorded so LEAF can reaccess them later.
- Updated relevant agent instructions and memory for Conductor, Data Scientist, Content Reader, and Context Mapper to reflect this rule.
- User provided a CSV of BookRoll operation/event names and definitions; stored these as working canonical definitions for future analysis memory.
- Added a LEAF Data Scientist rule: before analysis, check whether relevant data volume/coverage is sufficient; if not, do not use that dataset for that analysis and report the limitation.
- Recorded that analysis student score data is in `analysis_development.course_student_scores`.
- Recorded that BookRoll xAPI ClickHouse `actor_account_name` values commonly look like `<moodle_user_id>@<tenant_or_uuid>`; the prefix before `@` matches Moodle `user.id` and can be used for cross-source user joins.
- Started a bounded read-only database exploration in `projects/leaf-db-exploration-2026-03-25/`.
- Early findings: `analysis_development.course_student_scores` is populated, Moodle user table is `mdl_user`, BookRoll has large behavioral tables, and ClickHouse exposes many per-instance xAPI databases with `statements` / `statements_mv` style tables.
- User clarified an operating rule: until told otherwise, use only ClickHouse database `saikyo_old`.
- User also instructed that for Moodle, BookRoll, and Analysis relational schemas, I should ask for table/column definitions when needed instead of guessing meanings.
- Ran a bounded first-pass scan of ClickHouse `saikyo_old.statements_mv`: about 101.8M rows, 7,939 accounts, 13,294 contents, 43 operations.
- The strongest preliminary research-angle candidates from `saikyo_old` are: annotation/memo behavior, navigation strategy patterns, recommendation uptake funnels, quiz-related behavior pathways, and content-level friction signatures.
- Important quality cautions in `saikyo_old`: a very large blank `operation_name` bucket and anomalous timestamps around 1970 and one row in 2149.
- User clarified that Moodle course information is important context because BookRoll contents basically belong to courses.
- Course analyses should consider Japanese K-12 grade/level and subject area (for example English, Japanese) rather than pooling all contents together blindly.
- Re-checked the top 3 candidate research angles against both Ogata-lab/adjacent work and broader outside literature.
- The best gap-filling direction is not generic learner profiling or generic recommendation uptake; the strongest current candidate is content-level friction diagnostics across Japanese K-12 courses using BookRoll interaction traces.
- Other viable gap-aware angles are course-design / instructional-misalignment analytics from navigation patterns and context-aware ignored-help / recommendation timing analysis.
- Drafted a proper research concept note for the top angle: content-level friction diagnostics across Japanese K-12 courses using BookRoll traces.
- The concept note frames the project around diagnosing likely high-friction contents, distinguishing productive deep engagement from difficulty, and using subject/grade/course context for interpretation.
- User added ClickHouse column-definition guidance for the analysis side: column names are generally literal, `_id` can contain duplicates and should not be blindly double-counted, `contents_id` is the BookRoll PDF/content ID and is unique among databases, `contents_name` is the BookRoll content name, and `time_from_last_activity` is the per-user gap since the previous action.
- Drafted a concrete pilot analysis plan for the content-friction study, including exact first ClickHouse queries, candidate variables, inclusion thresholds, provisional friction-score logic, and the first figure/table set.