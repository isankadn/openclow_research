# 2026-03-26

- User expanded the xAPI exploration scope beyond `saikyo_old` and explicitly requested a sufficiency check on ClickHouse `saikyo_new.statements_mv`, with no MySQL/BookRoll/Analysis inspection.
- Bounded read-only profile of `saikyo_new.statements_mv`:
  - `7,636,461` rows
  - `2,306` unique users
  - `3,969` unique contents
  - `160` unique operation names
  - time range includes anomalies around `1969-12-31` / `1970-01-01`, but substantive activity is concentrated in `2025`-`2026`
  - blank `operation_name` rows: `557,190`
  - blank `contents_id` rows: `843,574`
- Coverage for the prior xAPI-only content-friction style pilot appears sufficient in `saikyo_new`:
  - `1,221` contents meet the old pilot threshold (`uniq_users >= 30` and `events >= 300`)
  - `2,210` contents meet a looser threshold (`uniq_users >= 10` and `events >= 100`)
- Main caution for `saikyo_new`: much smaller than `saikyo_old`, with noticeable blank-field buckets and likely platform/non-content events mixed in, so rare subgroup analyses may still be underpowered.
