# 2026-05-22

- User questioned the large exclusion of 59,209,738 old Bookroll/xAPI events and asked for all SQL queries used across databases. Audited scripted SQL in the LEAF grade-test and course-context projects and created `projects/leaf-grade-test-analysis-2026-05-19/sql_query_inventory.md`, grouping queries by analysis MySQL, LMS/Moodle MySQL, old Bookroll MySQL, old ClickHouse xAPI, and new ClickHouse xAPI. Key finding: the 59,209,738 events were fetched by the old content-month xAPI query and then excluded locally because their `contents_id` lacked a unique course mapping in the old Bookroll content-directory bridge; onboarding them requires improving content-course mapping, not changing the xAPI extraction query alone.
- User asked whether separate new data-store host/access information exists for relational MySQL/Bookroll/Moodle/Analysis. Current workspace memory records OLD relational MySQL endpoints on `10.236.173.145` and ClickHouse access on `10.236.173.4` for both `saikyo_old` and `saikyo_new` xAPI, but no separate post-2025/new relational MySQL host/access information is currently recorded.
- User clarified that analyses should include the 2025-04-01 to 2026-04-01 period and provided NEW relational MySQL endpoints: new Bookroll `localhost:30100`, new Analysis `localhost:30101`, and new Moodle `localhost:30102`. Updated `MEMORY.md`, `memory/leaf-old-data-stores.md`, and `agents/leaf-data-scientist/MEMORY.md` so future analysis includes these stores.
- User emphasized that `agents/leaf-data-scientist/MEMORY.md` already contains the new MySQL access details and instructed: use agents for analysis jobs, or obtain/read the Data Scientist agent memory first. Added this as an operating rule in main `MEMORY.md` and `agents/leaf-conductor/MEMORY.md`.
- User asked to rerun the full analysis but start with the manuscript-excluded 59,209,738 old Bookroll events and provide relevant SQL for manual mapping review. Read Data Scientist memory, spawned an analysis worker, verified NEW MySQL metadata on localhost ports via `127.0.0.1`, and created `projects/leaf-grade-test-analysis-2026-05-19/skipped_old_events_sql_review_2026-05-22.md` with the exact old xAPI extraction query, current old Bookroll bridge query, local exclusion logic, and candidate old/new mapping SQL including `tmp_sk_user_course`, Moodle LTI placement, Analysis `contents`, quizzes, and marker text evidence.
