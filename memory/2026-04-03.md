# 2026-04-03

- User clarified semantics for `analysis_development.course_student_scores`: `name` = test name, `course_name` = course name, `course_id` = course ID, and `date_at` = test date / date conducted.
- Ran a bounded read-only accessibility check against `analysis_development.course_student_scores`.
- Confirmed the table is accessible and returns data.
- Quick profile: 67,672 rows total, 2,497 distinct students, 226 distinct courses, 1,070 distinct test names, and 43,180 rows with non-null `date_at`.
- Sample accessible rows include records like `1A 後期期末　英語` in course `2022年度1年A組英語[中学]` with `course_id=397` and `date_at=2023-02-21`.
- Orchestrated 3 sub-agents to design score-data integration: integrity audit, analysis design, and schema-clarity checklist.
- Merged sub-agent findings into a validity-first plan: start with an Analysis-only clean score layer, then controlled course-level xAPI enrichment, then student-level xAPI enrichment only after identity mapping is verified.
- Phase 1 integrity checks showed: 24,492 / 67,672 score rows (36.19%) have non-null `course_student_id`, and all such rows match `course_students.id`.
- `course_students` has 305 duplicate `(student_id, course_id)` pairs, so fallback joining on `(student_id, course_id)` is not automatically safe.
- `course_student_scores` showed 0 duplicate groups at `(student_id, course_id, name, date_at)` and internally consistent course labeling (`course_id` <-> `course_name`).
- All 226 distinct score-table `course_id` values overlap Moodle `mdl_course.id`, strongly supporting a shared course-ID system there.
- Only 13 of the 226 score-table `course_id` values overlap non-empty `saikyo_new.statements_mv.context_id`, so current xAPI enrichment against `saikyo_new` is possible only for a narrow subset of courses.
- Saved a project note at `projects/leaf-score-integration-2026-04-03/phase1-integrity-checks.md`.
- Continued with a conservative phase 2 restricted enrichment on the 13 overlapping courses.
- Within those 13 courses, `course_students` coverage existed only for course `280`, so `course_student_id` / `course_students` bridging is insufficient for most overlapping score-bearing courses.
- Empirical student-ID overlap between score-table `student_id` and xAPI actor-prefix IDs exists in a few overlapping courses, but coverage is sparse.
- Under a strict validity rule (same course, exact student-ID match, xAPI before `date_at`), the restricted enrichment produced 0 valid matched pre-test score rows.
- The safe conclusion is that score-only longitudinal analysis is currently valid, but merged pre-test xAPI + score findings from `saikyo_new` are not yet defensible.
- Saved phase 2 results at `projects/leaf-score-integration-2026-04-03/phase2-restricted-enrichment-results.md`.
- Generated a first visual chart pack from score-only data in `projects/leaf-score-integration-2026-04-03/charts/`, including `index.html`, top-course and top-test bar charts, monthly score-row counts, and repeated-test structure.
